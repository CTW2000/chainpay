# 只给测试探针用的节点地址（进程拆分 ①，2026-09-23 从 local.env.example 挪出来）。
#
# 为什么单独一个文件：env/local.env 整份进容器的环境（docker-compose.yml 的 env_file），
# 而应用从不读这两个变量——放在那里，容器里就白白多了两把带 key 的钥匙。EnvInventoryTest 守着「进程的样例里没有它们」。
#
# 设了才会跑 SepoliaProbeTest / SepoliaIndexProbeTest（默认测试集必须离线）。用法：
#   cp env/probe.env.example env/probe.env     # 填好；env/*.env 不进 git
#   set -a; . env/probe.env; set +a
#   mvn test -Dtest='SepoliaProbeTest,SepoliaIndexProbeTest'
# 或者只在一条命令里给：
#   CHAINPAY_SEPOLIA_RPC=https://ethereum-sepolia-rpc.publicnode.com \
#   CHAINPAY_SEPOLIA_AUDIT_RPC=https://sepolia.gateway.tenderly.co \
#   mvn test -Dtest='SepoliaProbeTest,SepoliaIndexProbeTest'
CHAINPAY_SEPOLIA_RPC=
CHAINPAY_SEPOLIA_AUDIT_RPC=
